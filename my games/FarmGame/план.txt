I. Підготовка проєкту

Створи WPF-проєкт → FarmGame

Додай основні папки:

Models — логіка ігрових об’єктів (Поле, Амбар, Курятник, Культура, Тварина, тощо)

ViewModels — реалізація патерну MVVM

Views — XAML-вікна та сторінки (MainWindow, FieldPage, BarnPage і т.д.)

Services — збереження даних, таймери, економіка

Resources — зображення, іконки, UI-елементи

Налаштуй базовий інтерфейс:

Верхня панель → Монети 💰, Кристали 💎, XP ⭐, Рівень 📈

Центральна область → Карта ферми (поля, будівлі)

Нижня панель → Кнопки: “Посадити”, “Побудувати”, “Зібрати”, “Магазин”

II. Модель гри (Models)
1. Базові класи:

Player → монети, кристали, XP, рівень

Building (абстрактний) → назва, ціна, XP за побудову, побудовано/ні

Field : Building → кількість клітинок, що вирощується, список культур

Barn : Building → максимальна місткість, список продуктів

Crop → назва, час росту, XP за одиницю, кількість урожаю

Animal → назва, тип корму, XP, час виробництва продукту

FeedFactory : Building → рецепти корму

III. Ігрова логіка (Services)

LevelSystemService

Обчислення поточного рівня за XP

Відкриття нових культур/будівель

TimerService

Таймери росту культур (реальний час)

Оновлення UI після завершення

EconomyService

Купівля/продаж предметів

Облік вартості, нагород

StorageService

Перевірка місткості амбару

Додавання/видалення продукції

IV. 1 РІВЕНЬ

🔓 Відкривається:

Амбар

Поле мале №1

💡 Логіка:

Гравцеві з’являється повідомлення/вікно:
“Побудуйте амбар (+25 XP)” → додається об’єкт Barn.

Далі — “Побудуйте мале поле №1 (+25 XP)”.

При наведенні на поле → “Посадити пшеницю (⏱️ 1 хв.)”.

Через хвилину → пшениця дозріває → “Зібрати” → кожен колосок = +1 XP.

Урожай потрапляє в амбар (20 клітинок).

🎁 Нагорода: +500 монет, +5 кристалів.

V. 2 РІВЕНЬ (50 XP)

🔓 Відкривається:

Кукурудза (5 хв. росту, 2 XP за одиницю)

Курятник (вміщує 6 курей, будівництво за 200 монет)

Можна купити 2 курки (по 100 монет)

Завод кормів (рецепт: 3 пшениці + 2 кукурудзи → корм)

💡 Логіка:

Гравцеві з’являється повідомлення “Відкрито нові можливості!”

Доступна кнопка “Побудувати курятник”.

Курей можна годувати кормом із заводу.

Кури після годування через N хвилин приносять яйця (можеш визначити час).

🎁 Нагорода: +300 монет, +5 кристалів.

VI. Інтерфейс і візуальна логіка

Використай Canvas або Grid для відображення ферми.

Будівлі можуть бути Button або UserControl з іконками.

При натисканні на об’єкт → відкривається панель дій (“Посадити”, “Зібрати”, “Побудувати”).

Для анімацій — Storyboard або DispatcherTimer (плавне вирощування культур).

VII. Збереження прогресу

JSON або XML файли для збереження:

рівень, XP, кількість монет і кристалів

стан полів і будівель

таймери росту

VIII. Наступні кроки (після 2 рівня)

3 рівень: Молочна ферма 🐄

4 рівень: Млин (виробництво борошна)

5 рівень: Пекарня (виробництво хліба)

Додати систему квестів, торгівлі або NPC-сусідів.